PORT=5001
NODE_ENV=development
FRONTEND_URL=https://localhost:3002

# Database (when PostgreSQL is added)
# DATABASE_URL=postgresql://username:password@localhost:5432/cubes_db

# JWT Secret (for authentication if added later)
# JWT_SECRET=your-jwt-secret-here

# CORS settings
CORS_ORIGIN=https://localhost:3002