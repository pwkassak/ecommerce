# Traffic Simulator Configuration
# Copy this file to override default settings in docker-compose

# Number of concurrent browser sessions running simultaneously
CONCURRENT_BROWSERS=5

# Session duration range in seconds
SESSION_DURATION_MIN=30
SESSION_DURATION_MAX=120

# Target application URL (use container network name for docker)
TARGET_URL=http://frontend:3002

# Delay between launching new sessions (milliseconds)
RESTART_DELAY=2000

# Logging configuration
LOG_LEVEL=info

# Node environment
NODE_ENV=production

# Browser configuration
# Uncomment to enable additional browser options
# BROWSER_HEADLESS=true
# BROWSER_SLOW_MO=0

# Performance tuning
# MAX_ACTIONS_PER_SESSION=10
# MIN_ACTIONS_PER_SESSION=3